import { LibraryLayout } from '../../layouts';

/** Content library route — filters, search, and grid live in `LibraryLayout`. */
export default function ContentLibraryPage() {
  return <LibraryLayout />;
}
