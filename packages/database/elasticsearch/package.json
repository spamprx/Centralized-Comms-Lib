{
  "name": "@comms-lib/db-elasticsearch",
  "version": "0.0.1",
  "private": true,
  "description": "Elasticsearch client and content index mapping for Centralized Comms",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.15.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
