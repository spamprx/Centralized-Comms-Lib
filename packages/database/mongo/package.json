{
  "name": "@comms-lib/db-mongo",
  "version": "0.0.1",
  "private": true,
  "description": "MongoDB client and document payloads for Centralized Comms",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
